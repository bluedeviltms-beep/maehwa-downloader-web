services:
  - type: web
    name: maehwa-downloader-backend
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    region: singapore
    envVars:
      - key: PORT
        value: 10000
      - key: ALLOWED_ORIGIN
        value: "*"
